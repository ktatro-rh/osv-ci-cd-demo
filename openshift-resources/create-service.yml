apiVersion: v1
kind: Service
metadata:
  name: "{{ service_name }}"
  namespace: "{{ vm_project }}"
spec:
  selector:
    app: "{{ vm_name_set }}"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080